function soma(n1 = 0, n2 = 0){
    return n1 + n2
}

// variáveis para entrada de dados
let num1 = 11
let num2 = 10

// Variável para armazenar a saida de dados
let saida = soma(num1, num2) // Entrada de dados

console.log(`A soma entre os digítos ${num1} e ${num2} é ${saida}`)